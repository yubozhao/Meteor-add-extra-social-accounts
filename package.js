Package.describe({
  summary: "Add ability to add additionalsocial accounts without creating another user in Meteor.  "
});

Package.on_use(function (api, where) {
  api.use();

  api.add_files();
});

Package.on_test(function (api) {
});